import { LapseTimePipe } from './lapse-time.pipe';

describe('LapseTimePipe', () => {
  it('create an instance', () => {
    const pipe = new LapseTimePipe();
    expect(pipe).toBeTruthy();
  });
});
